const calc = require('./Calculator');

// const [operation, ...rest] = process.argv.slice(2);
// const numbers = rest.map(el => Number(el));
// console.log(calc.res(operation, numbers));
console.log(calc.init());
